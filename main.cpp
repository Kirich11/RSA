#include 'RSA.h';

int main() {
    
    /* pseudo alg 
       get message from user cin>>message
       autogenerate p,q or let user set them
       count phi()
       choose e where gcd(phi(), e) = 1
       open key = [n, e]
       find d where d*e = 1 (mod 20)
       secret key is d
    */
    return 0;
}